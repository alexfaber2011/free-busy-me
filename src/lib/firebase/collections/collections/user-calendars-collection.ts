import firebase from 'firebase/app';

export default class UserCalendarsCollection {
  // public collection: firebase.firestore.CollectionReference;
  // private users: firebase.firestore.CollectionReference;

  // TODO - determing how to organize user, and collections.
  // Thinking about using a Context
  // constructor(usersCollection: firebase.firestore.CollectionReference) {
  //   this.users = usersCollection;
  // };

  // async index(userId: string) {
//
//   }
}
